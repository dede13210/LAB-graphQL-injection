# LAB-graphQL-injection

run mongo database :
```
docker build -t my-mongo-db .
docker run -d -p 27017:27017 --name mongo-container my-mongo-db
```

run express server : 
```
npm install
npm run server
```
